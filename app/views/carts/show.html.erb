<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>
  <% if cart.empty? %>
    <%= render 'empty' %>
  <% else %>
    <%= render 'enhanced' %>
  <% end %>
</section>
